# How install 
1. drag `Disable-JS.crx`(Don't allow JS to execute requires page reload) or `Stop-JS.rcx`(Don't allow JS to execute doesn't require page reload) file into `browser://extensions` page
2. agree with everything
